# `.osc/workspace/`

这里放个人工作区（按用户区分）。`open-spec-code init -u <name>` 会创建：

- `.osc/workspace/<name>/`

你可以在里面放：

- `journal.md`：会话记录（建议摘要式，方便回溯）
- `notes.md`：临时思考/草稿

是否纳入版本控制由团队自行决定；如果不想入库，可以在项目 `.gitignore` 里添加相应规则。

